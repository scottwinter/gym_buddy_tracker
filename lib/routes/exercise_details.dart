import 'package:flutter/material.dart';

class ViewExerciseDetails extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Incline Dumbel Press"),
        ),
        body: Text("Exercise Detials")
    );
  }
}